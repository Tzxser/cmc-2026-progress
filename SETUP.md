# 部署与访问状态

记录日期：2026-09-21（UTC+08:00）。

## 已确认

独立仓库 `Tzxser/cmc-2026-progress` 已存在，默认分支为 main，用户创建时设为 public。本次保留该可见性，不更改权限。

初始化提交 `87a5e8a60d053701df6449d158dd341dd64acecf` 已通过 GitHub 读取核验。后续文字档案在同一 main 分支维护，最新内容与提交以仓库实际历史为准，不以旧聊天文件包代替。

本仓库用于学习记录与跨账户交接，不需要另行部署应用服务器。入口是 README.md，状态主记录为 PROGRESS.md。

## 尚未上传的原件

两份 PDF 和初测照片仍在 `cmc-2026-progress-authorized.zip` 文件包内，没有在本次提交中上传。materials 目录目前只存说明和校验值。不要把“档案已经写入”表述为“所有原始附件都已上传”。

原件待补路径与大小见 materials/README.md。补齐时保留原始字节，核对 SHA256SUMS.txt；确认后再同时更新 README.md、PROGRESS.md、SOURCES.md 和本文件的附件状态。

## 范围与接续

本次只向这个新仓库写入，不向 math-quests 或其他仓库提交，也不清理先前创建的旧分支。更换账户后先读 README.md 的接续指令；能读公开文件不等于具备写入权限。
