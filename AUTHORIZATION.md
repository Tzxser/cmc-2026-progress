# 默认执行授权

记录日期：2026-09-21（UTC+08:00）。

学习者明确表示：“你进行写入和创建吧，以后默认执行，我会给你对应权限的”。

## 范围

仅限独立仓库 `Tzxser/cmc-2026-progress`。允许创建该仓库并写入本项目的学习档案和资料。

以后在对话中提供新的学习进度、答卷、订正或复测结果时，默认同步更新本仓库，不再逐次询问是否保存。学习者可随时更改或撤回此授权。

## 不包含的操作

不包含写入其他仓库、删除历史、强推覆盖、改变已有公开性或账户权限。未修改整个 GitHub 插件的全局确认设置。

## 实际执行条件

当前账户必须具备对应工具和仓库访问权限。先读取最新记录，再更新、提交、读回核验，最后报告提交标识。失败时明确说未保存，不得以本地文件包代替云端保存证明。

这是对话中的更新授权，不是后台监控或自动定时同步。不会使其他账户自动获得访问权限。
